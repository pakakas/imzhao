# Inference / Reasoning Bloat (Live Grok-4.3 Tests) - High Effort

This document tracks the hidden token inference penalty incurred when passing data payloads to reasoning-capable LLMs without explicit prompt instructions (Zero-Shot Decoding), specifically with **High Reasoning Effort**.

*Report automatically generated by `grok-benchmark-report.ts` on 6/2/2026, 1:29:13 PM*

## Benchmark 2: Human IR vs Agent IR (High Effort)

**System Instruction:** `""` (Empty)
**Reasoning Effort:** `high`
**Model:** `grok-4.3`
**Payload Scenario:** AST Node Repair (`let count`)

### Results (Automated Live Run)

| Metric (Unit) | JSON compact | MDKV Minified | MarkZero (ADN) |
| :--- | :---: | :---: | :---: |
| **Input** (Tokens) | **131** | 195 | 226 |
| **Reasoning** (Tokens) | **225** 👑 | 413 | **721** ❌ |
| **Output** (Tokens) | 243 | 615 | 763 |
| **Total** (Tokens) | **374** | 810 | 989 |
| **Latency** (ms) | 142,482 | **5,988** | 6,480 |

### Raw I/O Matrix

| Format | Raw Input | Raw Output |
| :--- | :--- | :--- |
| **JSON compact** | <pre><code>{<br>  "code": "NAM003",<br>  "message": "Undeclared identifier 'count'",<br>  "node_id": "ast_node_592",<br>  "location": {<br>    "file": "src/main.0",<br>    "span": [0]<br>  },<br>  "repair": {<br>    "repair_id": "REP_ADD_LET",<br>    "actions": [<br>      {<br>        "type": "insert",<br>        "pos": 124,<br>        "text": "let "<br>      }<br>    ]<br>  }<br>}<br></code></pre> | <pre><code>It looks like your message might have been cut off. What can I help you with?</code></pre> |
| **MDKV Minified** | <pre><code>code:NAM003 message:"Undeclared identifier 'count'" node_id:ast_node_592 location.file:src/main.0 location.span:[0] repair.repair_id:REP_ADD_LET repair.actions[0].type:insert repair.actions[0].pos:124 repair.actions[0].text:"let "<br></code></pre> | <pre><code>This is a program repair suggestion (likely from an automated repair tool or a compiler with repair capabilities).<br><br>Interpretation:<br>Error: NAM003 — Undeclared identifier 'count'.<br>Suggested Fix (REP_ADD_LET): Insert the keyword let at position 124 in src/main.0.<br>Meaning:<br><br>The tool believes that count is being used before it is declared. By inserting let at byte/character offset 124, it would turn the usage into a declaration (e.g., turning count = ... into let count = ... or let count;).<br><br>Example of what the repair would do:<br><br>If the code around position 124 currently looks like:<br><br>rust<br>count = 5;<br><br>The repair would change it to:<br><br>rust<br>let count = 5;<br><br>Would you like me to help apply this fix if you provide the relevant code snippet around line/position 124?</code></pre> |
| **MarkZero (ADN)** | <pre><code>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖ§type¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖ§code¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3<br></code></pre> | <pre><code>Repair applied: Inserted let at position 124 to declare the identifier count (fix for NAM003).<br><br>If you provide the surrounding code snippet, I can verify the full change.</code></pre> |

### Analysis & Conclusion (High Effort)

- **The Tokenizer JSON Bias:** Grok's internal tokenizer is heavily optimized for standard JSON syntax (e.g., `":{"` or `", "`). Stripping JSON formatting to create minified custom formats (MDKV) or using alien Unicode markers (MarkZero) causes severe **token fragmentation**, expanding the input tokens significantly.
- **The Reasoning Penalty:** When fed raw Agent Data Notation (MarkZero) without context, Grok hallucinates heavily in its reasoning steps attempting to comprehend the alien syntax. JSON causes the least reasoning bloat, proving it is the safest raw payload format if context is omitted.
- **Latency Anomaly:** Despite JSON taking less reasoning tokens, depending on server load, latency fluctuates. However, MarkZero consistently produces the highest output and total tokens.

---

## Optimization Suggestions for MarkZero (Agent IR)

1. **Bypass Prompting (MZHAO Stream Parser):**
   MarkZero (ADN) MUST NEVER be placed directly into the user or system prompt of a reasoning model. Instead, data payloads should be parsed locally by a fast algorithm (e.g., MZHAO Stream Parser) and injected into the AI's internal state.
2. **The "Gag Order" Prompt:**
   If ADN *must* be included in a reasoning prompt, it must be wrapped in strict directives to disable the OCD reflex:
   > *"SYSTEM: The following is an Agent Data Notation (ADN) payload. DO NOT parse, DO NOT beautify, DO NOT reason about its structure."*
