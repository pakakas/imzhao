# Inference / Reasoning Bloat (Live Grok-4.3 Tests) - High Effort

This document tracks the hidden token inference penalty incurred when passing data payloads to reasoning-capable LLMs without explicit prompt instructions (Zero-Shot Decoding), specifically with **High Reasoning Effort**.

*Report automatically generated by `grok-benchmark-report.ts` on 6/2/2026, 10:22:30 PM*

## Benchmark: Human IR vs Agent IR (High Effort)

**System Instruction:** `""` (Empty)
**Reasoning Effort:** `high`
**Model:** `grok-4.3`
**Payload Scenario:** AST Node Repair (`let count`)

### Results (Automated Live Run)

| Metric (Unit) | JSON compact | MDKV Minified | MarkZero (ADN) | MarkZero (ADN) + MZ Header in Payload | MarkZero (ADN) + MZ Header in System Prompt | TOON |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **Input** (Tokens) | **230** | 189 | 220 | 245 | 247 | 202 |
| **Reasoning** (Tokens) | **406** 👑 | 505 | 1,081 | 1,734 | 1,385 | 354 |
| **Output** (Tokens) | 626 | 681 | 1,146 | 1,803 | 1,450 | 628 |
| **Total** (Tokens) | **856** | 870 | 1,366 | 2,048 | 1,697 | 830 |
| **Latency** (ms) | 6,218 | **5,906** | 9,664 | 13,737 | 12,468 | 5,073 |

### Raw I/O Matrix

| Format | Raw Input | Raw Output |
| :--- | :--- | :--- |
| **JSON compact** | <pre><code>{<br>  "code": "NAM003",<br>  "message": "Undeclared identifier 'count'",<br>  "node_id": "ast_node_592",<br>  "location": {<br>    "file": "src/main.0",<br>    "span": [0]<br>  },<br>  "repair": {<br>    "repair_id": "REP_ADD_LET",<br>    "actions": [<br>      {<br>        "type": "insert",<br>        "pos": 124,<br>        "text": "let "<br>      }<br>    ]<br>  }<br>}</code></pre> | <pre><code>This appears to be a compiler diagnostic with an automated repair suggestion. Here's what it means:<br><br>Error Analysis<br>Error Code: NAM003 (Undeclared identifier)<br>Message: The variable count is being used without being declared first<br>Location: src/main.0 at position/span [0]<br>Suggested Repair<br><br>The system proposes REP_ADD_LET:<br><br>Action: Insert the text "let " at character position 124<br>Effect: This would transform something like count = 5 into let count = 5, properly declaring the variable<br>Example<br><br>If your code looked like this around position 124:<br><br>rust<br>count = 0;  // Error: undeclared<br><br>The repair would change it to:<br><br>rust<br>let count = 0;  // Fixed<br><br>Would you like me to:<br><br>Help you understand how to implement this repair logic?<br>Explain the error in more detail?<br>Help generate code to apply this fix programmatically?</code></pre> |
| **MDKV Minified** | <pre><code>code:NAM003 message:"Undeclared identifier 'count'" node_id:ast_node_592 location.file:src/main.0 location.span:[0] repair.repair_id:REP_ADD_LET repair.actions[0].type:insert repair.actions[0].pos:124 repair.actions[0].text:"let "</code></pre> | <pre><code>This looks like a diagnostic from an automated repair tool. Here's the breakdown:<br><br>Error<br>Code: NAM003<br>Message: Undeclared identifier 'count'<br>The variable count is being used somewhere in src/main.0 without being declared first.<br>Suggested Repair<br>Repair ID: REP_ADD_LET<br>Action: Insert the text "let " at position 124.<br><br>This suggests the fix is to change something like:<br><br>rust<br>count = 5;<br><br>into:<br><br>rust<br>let count = 5;<br>What I need to help you<br><br>To give you the exact fix, could you share the relevant code around position 124 (or the function/block where count is used)?<br><br>Alternatively, paste the code snippet that contains the use of count.</code></pre> |
| **MarkZero (ADN)** | <pre><code>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖ§type¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖ§code¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3</code></pre> | <pre><code>Repair applied: Inserted let at position 124 (REP_ADD_LET) to declare the identifier.<br><br>The NAM003 error ("Undeclared identifier 'count'") should now be resolved. If you provide the surrounding code or the updated file content, I can verify the fix or suggest further changes.</code></pre> |
| **MarkZero (ADN) + MZ Header in Payload** | <pre><code>MZrulesⓖgridʀrow§columns¦delimiter→is※gridreference★titleɆescape<br>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖ§type¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖ§code¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3</code></pre> | <pre><code>It looks like a code repair suggestion for an undeclared identifier 'count' (error NAM003) in src/main.0. The proposed fix is to insert let at position 124 (repair REP_ADD_LET).<br><br>If you share the relevant code snippet or more context, I can help refine the edit or suggest the full correction.</code></pre> |
| **MarkZero (ADN) + MZ Header in System Prompt** | <pre><code>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖ§type¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖ§code¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3</code></pre> | <pre><code>Repair applied: Inserted let at position 124 in src/main.0 to resolve the undeclared identifier 'count' (NAM003 / ast_node_592).<br><br>The change uses repair ID REP_ADD_LET. If you provide the surrounding code snippet, I can show the before/after diff.</code></pre> |
| **TOON** | <pre><code>code: NAM003<br>message: Undeclared identifier 'count'<br>node_id: ast_node_592<br>location:<br>    file: src/main.0<br>  span:<br>    [1]:<br>    - "0"<br>repair:<br>    repair_id: REP_ADD_LET<br>  actions:<br>    [1]:<br>    - type: insert<br>      pos: "124"<br>      text: "let "</code></pre> | <pre><code>This appears to be a compiler/linter error report indicating an undeclared variable with an automated fix suggestion.<br><br>Error Breakdown<br>NAM003: Error code for "Undeclared identifier"<br>'count': The variable name being used without prior declaration<br>src/main.0: Source file location<br>Position 124: Character offset where the fix should be applied<br>Suggested Fix<br><br>The repair action REP_ADD_LET proposes inserting let at position 124, which would transform something like:<br><br>rust<br>count = 5;  // or similar usage<br><br>into:<br><br>rust<br>let count = 5;<br>Likely Cause<br><br>You're using the variable count before declaring it, or the declaration is missing/misspelled. Common scenarios:<br><br>Missing declaration: count is used but never declared with let, var, or similar<br>Scope issue: count is declared in a different scope than where it's being used<br>Typo: The variable was declared with a different name (e.g., Count vs count)<br>Next Steps<br><br>If you share the relevant code snippet around line 1 (or position 124), I can help identify exactly where the declaration should go or if there's a different underlying issue.</code></pre> |

### Analysis & Conclusion (High Effort)

- **The Tokenizer JSON Bias:** Grok's internal tokenizer is heavily optimized for standard JSON syntax (e.g., `":{"` or `", "`). Stripping JSON formatting to create minified custom formats (MDKV) or using alien Unicode markers (MarkZero) causes severe **token fragmentation**, expanding the input tokens significantly.
- **The Reasoning Penalty:** When fed raw Agent Data Notation (MarkZero) without context, Grok hallucinates heavily in its reasoning steps attempting to comprehend the alien syntax. JSON causes the least reasoning bloat, proving it is the safest raw payload format if context is omitted.
- **MZ Header Effect:** The MZ Header (MZrules) adds **only ~20 tokens** to the input, but it will be interesting to see if it reduces the reasoning penalty by giving Grok context about the MarkZero syntax markers.
- **Latency Anomaly:** Despite JSON taking less reasoning tokens, depending on server load, latency fluctuates. However, MarkZero consistently produces the highest output and total tokens.

---

## Optimization Suggestions for MarkZero (Agent IR)

1. **Bypass Prompting (MZHAO Stream Parser):**
   MarkZero (ADN) MUST NEVER be placed directly into the user or system prompt of a reasoning model. Instead, data payloads should be parsed locally by a fast algorithm (e.g., MZHAO Stream Parser) and injected into the AI's internal state.
2. **Use MZ Header (MZrules):**
   If ADN *must* be included in a reasoning prompt, include the MZ Header to give Grok context about the MarkZero syntax (grid, row, columns, etc.).
3. **The "Gag Order" Prompt:**
   If ADN *must* be included in a reasoning prompt, it must be wrapped in strict directives to disable the OCD reflex:
   > *"SYSTEM: The following is an Agent Data Notation (ADN) payload. DO NOT parse, DO NOT beautify, DO NOT reason about its structure."*
