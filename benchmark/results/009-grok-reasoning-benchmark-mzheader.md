# Inference / Reasoning Bloat (Live Grok-4.3 Tests) - High Effort

This document tracks the hidden token inference penalty incurred when passing data payloads to reasoning-capable LLMs without explicit prompt instructions (Zero-Shot Decoding), specifically with **High Reasoning Effort**.

*Report automatically generated by `grok-benchmark-report.ts` on 6/2/2026, 9:13:37 PM*

## Benchmark: Human IR vs Agent IR (High Effort)

**System Instruction:** `""` (Empty)
**Reasoning Effort:** `high`
**Model:** `grok-4.3`
**Payload Scenario:** AST Node Repair (`let count`)

### Results (Automated Live Run)

| Metric (Unit) | JSON compact | MDKV Minified | MarkZero (ADN) | MarkZero (ADN) + MZ Header in Payload | MarkZero (ADN) + MZ Header in System Prompt |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **Input** (Tokens) | **230** | 189 | 220 | 245 | 247 |
| **Reasoning** (Tokens) | **294** 👑 | 373 | **1,015** ❌ | 1,164 | 994 |
| **Output** (Tokens) | 543 | 619 | 1,092 | 1,286 | 1,097 |
| **Total** (Tokens) | **773** | 808 | 1,312 | 1,531 | 1,344 |
| **Latency** (ms) | 54,863 | **7,353** | 14,572 | 11,055 | 10,093 |

### Raw I/O Matrix

| Format | Raw Input | Raw Output |
| :--- | :--- | :--- |
| **JSON compact** | <pre><code>{<br>  "code": "NAM003",<br>  "message": "Undeclared identifier 'count'",<br>  "node_id": "ast_node_592",<br>  "location": {<br>    "file": "src/main.0",<br>    "span": [0]<br>  },<br>  "repair": {<br>    "repair_id": "REP_ADD_LET",<br>    "actions": [<br>      {<br>        "type": "insert",<br>        "pos": 124,<br>        "text": "let "<br>      }<br>    ]<br>  }<br>}<br></code></pre> | <pre><code>This appears to be a compiler/linter diagnostic (likely from a static analysis tool) reporting an undeclared variable error. Here's what each field means:<br><br>Error Details:<br><br>Code: NAM003 — Name/identifier error #003<br>Message: Undeclared identifier 'count' — The variable count is being used without being declared first<br>Location: src/main.0, span [0] — The error occurs in that file<br><br>Suggested Repair:<br><br>Strategy: REP_ADD_LET — The tool recommends declaring the variable<br>Action: Insert the text "let " at character position 124 in the source file<br><br>What this likely means: The code is using count (probably something like count = count + 1 or print(count)) without first declaring it with let count (or var count depending on the language syntax).<br><br>To apply the fix: At position 124 in src/main.0, insert let so the identifier becomes a proper variable declaration.<br><br>Would you like help interpreting this for a specific programming language, or do you need to understand how to implement this repair programmatically?</code></pre> |
| **MDKV Minified** | <pre><code>code:NAM003 message:"Undeclared identifier 'count'" node_id:ast_node_592 location.file:src/main.0 location.span:[0] repair.repair_id:REP_ADD_LET repair.actions[0].type:insert repair.actions[0].pos:124 repair.actions[0].text:"let "<br></code></pre> | <pre><code>This looks like a compiler/static analysis error report (likely from a Rust-related tool) with an automated repair suggestion.<br><br>Error Breakdown:<br>Code: NAM003<br>Message: Undeclared identifier 'count'<br>File: src/main.0<br>Suggested Repair: REP_ADD_LET<br>Action: Insert "let " at position 124<br>What this means:<br><br>The code is using the variable count without declaring it first. In Rust, variables must be declared with let (or let mut if mutable).<br><br>The repair tool is suggesting that at position 124, the code probably looks something like:<br><br>rust<br>count = 5;        // or some usage of count<br><br>And it wants to change it to:<br><br>rust<br>let count = 5;<br>Recommendation:<br><br>You should check the code around position 124 in src/main.0. The identifier count is likely being assigned or used before it's declared.<br><br>Would you like me to help you fix this? If you paste the relevant code section (around where count is used), I can give you the exact correction.</code></pre> |
| **MarkZero (ADN)** | <pre><code>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖᴄtype¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖᴄcode¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3<br></code></pre> | <pre><code>Repair applied: REP_ADD_LET<br><br>File: src/main.0<br>Location: position 124<br>Change: insert let before count<br>Error fixed: NAM003 – Undeclared identifier 'count'<br><br>The identifier count should now be properly declared. Let me know if you need the full diff or further changes.</code></pre> |
| **MarkZero (ADN) + MZ Header in Payload** | <pre><code>MZrulesⓖgridʀrowᴄcolumns¦delimiter→is※gridreference★titleɆescape<br>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖᴄtype¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖᴄcode¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3<br></code></pre> | <pre><code>Understood. Repair action parsed:<br><br>File: src/main.0<br>Action: REP_ADD_LET (insert let at position 124)<br>Error: NAM003 — Undeclared identifier 'count' (ast_node_592)<br><br>Suggested fix: Insert let at the indicated position so the identifier becomes properly declared (e.g. let count = ...).<br><br>Would you like me to:<br><br>Generate the exact patched snippet around position 124, or<br>Provide the full corrected code block once you share the surrounding context?</code></pre> |
| **MarkZero (ADN) + MZ Header in System Prompt** | <pre><code>ⓖ124ʀ129ⓖʀfile→src/main.0ʀspan→※0ⓖᴄtype¦pos¦textʀinsert¦124¦let ⓖʀrepair_id→REP_ADD_LETʀactions→※2ⓖᴄcode¦message¦node_id¦location¦repairʀNAM003¦Undeclared identifier 'count'¦ast_node_592¦※1¦※3<br></code></pre> | <pre><code>Repair acknowledged<br><br>File: src/main.0<br>Repair ID: REP_ADD_LET<br>Action: Insert let at position 124<br>Error: NAM003 — Undeclared identifier 'count' (node ast_node_592)<br><br>This change should turn the usage of count into a proper declaration. If you paste the relevant code snippet (or the lines around position 124), I can verify the fix or suggest the full corrected statement.</code></pre> |

### Analysis & Conclusion (High Effort)

- **The Tokenizer JSON Bias:** Grok's internal tokenizer is heavily optimized for standard JSON syntax (e.g., `":{"` or `", "`). Stripping JSON formatting to create minified custom formats (MDKV) or using alien Unicode markers (MarkZero) causes severe **token fragmentation**, expanding the input tokens significantly.
- **The Reasoning Penalty:** When fed raw Agent Data Notation (MarkZero) without context, Grok hallucinates heavily in its reasoning steps attempting to comprehend the alien syntax. JSON causes the least reasoning bloat, proving it is the safest raw payload format if context is omitted.
- **MZ Header Effect:** The MZ Header (MZrules) adds **only ~20 tokens** to the input, but it will be interesting to see if it reduces the reasoning penalty by giving Grok context about the MarkZero syntax markers.
- **Latency Anomaly:** Despite JSON taking less reasoning tokens, depending on server load, latency fluctuates. However, MarkZero consistently produces the highest output and total tokens.

---

## Optimization Suggestions for MarkZero (Agent IR)

1. **Bypass Prompting (MZHAO Stream Parser):**
   MarkZero (ADN) MUST NEVER be placed directly into the user or system prompt of a reasoning model. Instead, data payloads should be parsed locally by a fast algorithm (e.g., MZHAO Stream Parser) and injected into the AI's internal state.
2. **Use MZ Header (MZrules):**
   If ADN *must* be included in a reasoning prompt, include the MZ Header to give Grok context about the MarkZero syntax (grid, row, columns, etc.).
3. **The "Gag Order" Prompt:**
   If ADN *must* be included in a reasoning prompt, it must be wrapped in strict directives to disable the OCD reflex:
   > *"SYSTEM: The following is an Agent Data Notation (ADN) payload. DO NOT parse, DO NOT beautify, DO NOT reason about its structure."*
